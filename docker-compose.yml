version: "3"
services:
  frontend:
    build: ./frontend
    ports:
      - "4201:80"
    networks:
      - overlay
    depends_on:
      - backend
  backend:
    build: ./backend
    networks:
      - overlay

networks: 
  overlay:
